# generated files (tracked, but not hand-edited)
pnpm-lock.yaml
docs/api/
packages/db/migrations/

# generated and gitignored. Listed here as well rather than relying on
# Prettier's default of also reading .gitignore, so `format:check` does not
# depend on that default staying put.
**/.react-router/
**/.wrangler/
